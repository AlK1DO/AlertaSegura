<?xml version="1.0" encoding="utf-8"?>
<!-- res/navigation/nav_main.xml -->
<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_main"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.alertasegura.ui.home.HomeFragment"
        android:label="Inicio">
        <!-- Desde home se puede ir a Premium cuando se agota el límite -->
        <action
            android:id="@+id/action_home_to_premium"
            app:destination="@id/premiumFragment"
            app:enterAnim="@anim/slide_in_up"
            app:exitAnim="@anim/fade_out"
            app:popEnterAnim="@anim/fade_in"
            app:popExitAnim="@anim/slide_out_down"/>
    </fragment>

    <fragment
        android:id="@+id/contactsFragment"
        android:name="com.example.alertasegura.ui.contacts.ContactsFragment"
        android:label="Contactos"/>

    <fragment
        android:id="@+id/communityFragment"
        android:name="com.example.alertasegura.ui.community.CommunityFragment"
        android:label="Comunidad"/>

    <fragment
        android:id="@+id/mapFragment"
        android:name="com.example.alertasegura.ui.map.MapFragment"
        android:label="Mapa"/>

    <fragment
        android:id="@+id/profileFragment"
        android:name="com.example.alertasegura.ui.profile.ProfileFragment"
        android:label="Perfil">
        <action
            android:id="@+id/action_profile_to_premium"
            app:destination="@id/premiumFragment"
            app:enterAnim="@anim/slide_in_up"
            app:exitAnim="@anim/fade_out"
            app:popEnterAnim="@anim/fade_in"
            app:popExitAnim="@anim/slide_out_down"/>
    </fragment>

    <!-- Pantalla Premium: accesible desde Home y Perfil -->
    <fragment
        android:id="@+id/premiumFragment"
        android:name="com.example.alertasegura.ui.premium.PremiumFragment"
        android:label="Premium"/>

</navigation>